const config = {
    apiBaseUrl: process.env.REACT_APP_API_BASE_URL || 'http://localhost:5000/api', // Default API URL
    // Add other configuration variables as needed
  };
  
  export default config;